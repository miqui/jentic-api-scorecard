{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "types": ["node"]
  },
  // Library (Node) build only. The React SPA under src/app/ is built by Vite and
  // tsconfig.react.json — excluded here so JSX never reaches the Node compile.
  "include": ["src/index.ts"]
}
